import React from 'react';

import Divider from "../elements/divider"

const Hero = ({ offset, factor = 1 }) => {
    return (
        <div>
            HERO
        </div>
    );
};

export default Hero;